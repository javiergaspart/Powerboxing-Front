import 'package:flutter/material.dart';

class SessionManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Session Management Screen'));
  }
}
