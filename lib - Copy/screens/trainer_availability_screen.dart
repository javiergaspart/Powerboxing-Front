import 'package:flutter/material.dart';

class TrainerAvailabilityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Trainer Availability Screen'));
  }
}
